<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FinalProject.Dark" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/action</item>
        <item name="colorPrimaryDark">@color/fillPrimary</item>
        <item name="android:windowBackground">@color/fillPrimary</item>
        <item name="colorOnPrimary">@color/fillPrimary</item>
        <item name="colorPrimaryContainer">@color/fillPrimary</item>
        <item name="colorSecondaryContainer">@color/fillSecondary</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorAccent">@color/action</item>
        <item name="fontFamily">@font/alegreya_sans</item>
        <item name="android:textColor">@color/primary</item>
    </style>

    <style name="roundedImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
</resources>